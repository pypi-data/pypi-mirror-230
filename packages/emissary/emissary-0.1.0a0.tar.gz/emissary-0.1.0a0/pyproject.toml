[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "emissary"
version = "0.1.0-alpha"
description = "A user-friendly way to interact with command line interfaces."
readme = "README.md"
requires-python = ">=3.8"
license = {file = "LICENSE"}
keywords = ['python3', 'python', 'cli', 'cli wrapper']
authors = [
	{ name="Jesse C. Sealand", email="jesse.sealand@outlook.com" }
	]
#maintainers
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
	]
dependencies = []

#dynamic
#entry-points
#gui-scripts

[project.optional-dependencies]
#gui = ["PyQt5"]

[project.urls]
"Homepage" = "https://github.com/jesse-sealand/emissary"
"Bug Tracker" = "https://github.com/jesse-sealand/emissary/issues"

[project.scripts]
#spam-cli = "spam:main_cli"

[project.gui-scripts]
#spam-gui = "spam:main_gui"

[project.entry-points."spam.magical"]
#tomatoes = "spam:main_tomatoes"

[tool.setuptools.packages.find]
include = ["emissary"]
exclude = ["conda"]