from setuptools import setup, find_packages

setup(
    name="openai_pricing_draft",
    version="0.5",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
)