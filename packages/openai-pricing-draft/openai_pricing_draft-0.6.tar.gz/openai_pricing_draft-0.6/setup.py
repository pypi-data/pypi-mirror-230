from setuptools import setup, find_packages

setup(
    name="openai_pricing_draft",
    version="0.6",
    packages=find_packages(),
    py_modules=['pricing_calc'],
    install_requires=[
        # List your dependencies here
    ],
)