from setuptools import setup

# Metadata goes in setup.cfg. These are here for GitHub's dependency graph.
setup(
    name="eaxml2code",
    install_requires=[
        'mako>=1.1.6',
    ],
    extras_require={
    },
)