[tool.poetry]
name = "peb"
version = "0.0.10"
description = "Python Extensions Bundle"
authors = ["Jrog <jrog612@gmail.com>"]
license = "MIT"
readme = 'README.md'
homepage = 'https://github.com/jrog612/djackal'
repository = 'https://github.com/jrog612/peb'
packages = [
    { include = 'peb' }
]
exclude = ['test*', '.*']


[tool.poetry.dependencies]
python = "^3.5"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
