[tool.poetry]
name = "patch-target"
version = "0.1.2"
description = "Create unittest.mock.patch target values from python modules instead of string wrangling"
authors = ["brad.boggs <bradleywboggs@protonmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[tool.poetry.group.dev.dependencies]
pytest = "^7.4.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
