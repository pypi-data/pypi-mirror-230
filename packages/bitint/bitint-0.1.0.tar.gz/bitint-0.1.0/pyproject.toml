[tool.poetry]
name = "bitint"
version = "0.1.0"
description = "Pure Python implementation of extended integer with named bits."
authors = ["Robin Gottfried <bitint@kebet.cz>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
