[tool.poetry]
name = "prompt-functions"
version = "0.1.27"
description = "Build semantic functions."
authors = ["Kasper Junge"]
license = "MIT"
readme = "README.md"
packages = [{include = "prompt_functions"}]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.31.0"
aiohttp = "^3.8.5"
asyncio = "^3.4.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
