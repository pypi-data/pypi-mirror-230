[tool.poetry]
name = "liveo_perfect_utils"  # Required
version = "2.8.8"  # Required
description = "A dummy project"  # Optional
readme = "README.md" # Optional
license = "LICENSE.txt"
authors = ["A Random Developer <laxmi@live-eo.com>"]
packages = [
    { include = "liveeo" }
]


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
